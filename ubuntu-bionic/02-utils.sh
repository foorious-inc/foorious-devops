# install useful utils
sudo apt-get install git htop tree -y

# install other utils
sudo apt-get install net-tools -y # provides "ifconfig" command, etc.
sudo apt install software-properties-common # to add PPAs
sudo apt-get install apt-utils -y
